name: spordle
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.1

dependencies:
  flutter:
    sdk: flutter
  fluttertoast: ^8.2.8
  shared_preferences: ^2.0.6
  audioplayers: ^6.1.0
  just_audio: ^0.9.40
  assets_audio_player: ^3.1.1


  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/mystery.jpg
    - assets/akinacd87.jpeg
    - assets/akinaheadphones.jpg
    - assets/akinapossibility.jpg
    - assets/akinaprologue.jpg
    - assets/cachorro-irado.jpg
    - assets/chisatomite.jpg
    - assets/chisatothemoritaka.jpg
    - assets/meiko303east60thst.jpg
    - assets/meikopuzzle.jpg
    - assets/meikorussianroulette.jpg
    - assets/mihoonnaninaare.jpg
    - assets/Styley! Vol 2.png
    - assets/oginomekaigaraterasu.jpg
    - assets/oginomeraspberrywind.jpg
    - assets/oginomevergeoflove.jpg
    - assets/yoshieaishuu.png
    - assets/yoshieharunanoni.jpg
    - assets/yuicrystaleyes.jpg
    - assets/yuimelodyfair.jpg
    - assets/yuipresent.jpg
    - assets/songs/Aice room(Nor) - Luv U.flac
    - assets/songs/01_01_フライミ_菅原圭.wav
    - assets/songs/01_03_変身 (feat. Dirty Androids).flac
    - assets/songs/PSYQUI - 就寝御礼.flac

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
